import {
  ChevronBackCircleOutline,
  ChevronForwardCircleOutline,
} from "react-ionicons";
import styled from "styled-components";

export const NavWrapper = styled.div`
  display: flex;
  margin-left: 30px;
  cursor: pointer;
`;

export const NavFor = styled(ChevronBackCircleOutline)``;

export const NavBack = styled(ChevronForwardCircleOutline)``;
